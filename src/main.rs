mod beam_entry;
mod beam_state;
mod sorting;

use beam_entry::BeamEntry;
// use beam_state::BeamState;

fn main() {
    // Creating an instance with default values
    /*let mut entry = BeamEntry::default();
    entry.update_probabilities(0.3, 0.7);
    println!("pr_total: {}", entry.pr_total);
    println!("pr_non_blank: {}", entry.pr_non_blank);
    println!("pr_blank: {}", entry.pr_blank);

    // Creating an instance with custom values
    let mut custom_entry = BeamEntry::new(0.5, 0.5);
    custom_entry.update_probabilities(0.2, 0.3);
    println!("pr_total: {}", custom_entry.pr_total);
    println!("pr_non_blank: {}", custom_entry.pr_non_blank);
    println!("pr_blank: {}", custom_entry.pr_blank);*/
}
